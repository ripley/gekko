- Gekko API service is based on node.js and koa.
- If talib / tulib are installed but cannot be imported, do not use the --no-save flag.
    eg: npm install talib
        npm install tulind --build-from-source
